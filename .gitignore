node_modules/
*.log
.DS_Store
Thumbs.db
# Insurance: the workflow around this repo handles per-site .env files and
# licensed plugin zips — neither must ever land in a commit.
.env
*.zip
.katalyst.lock

# Playwright MCP scratch output
.playwright-mcp/

# Maintainer-private, local-only: session narratives and in-flight planning.
# These lived in the public repo until 2026-08-12; they stay on disk for the
# session workflow (CLAUDE.md references them) but are no longer tracked.
PLANNING/
HANDOFF.md
.handoffs/
